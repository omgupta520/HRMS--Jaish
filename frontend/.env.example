# The frontend talks to the backend through the Vite dev proxy (see vite.config.js),
# so no API URL is required for local development.
# For a production build that calls a remote API directly, you can set:
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
